applications:
- name: ((APP_NAME))
  instances: ((INSTANCES))
  memory: ((APP_MEMORY))
  routes:
    - route: rishabh_basic_app_addt.apps.pcf-harness.com
  services:
    - <+pipeline.variables.service>
