applications:
- name: ((APP_NAME))
  instances: ((INSTANCES))
  memory: ((APP_MEMORY))
  routes:
    - route: tanzusecretpassword3-kind-swan-ot.apps.tas-harness.com
  services:
    - <+pipeline.variables.service>
